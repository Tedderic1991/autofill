---
phase: 01-secure-foundation
plan: 07
type: execute
wave: 6
depends_on: [01-06]
autonomous: false
files_modified: []
requirements: [SEC-04, PROF-01, PROF-02, PROF-03, PROF-04, PROF-05, PROF-06, PROF-07, PROF-08, PROF-09]

must_haves:
  truths:
    - "App opens to biometric lock screen — no profile data visible without auth"
    - "Successful auth reveals profile list"
    - "User can create a profile with all built-in fields and a photo"
    - "User can create custom fields on a profile"
    - "Creating a third profile on free tier shows upgrade prompt, not an error"
    - "Profile data is encrypted on-disk: raw DB file is unreadable without the key"
    - "All unit and widget tests pass"
  artifacts:
    - path: "test/unit/"
      provides: "All unit test stubs implemented and green"
    - path: "test/widget/"
      provides: "All widget test stubs implemented and green"
  key_links:
    - from: "Physical Android device"
      to: "Biometric prompt"
      via: "app cold start → /auth route → local_auth.authenticate()"
      pattern: "manual"
    - from: "Create profile form"
      to: "Encrypted DB"
      via: "profileUseCase.createProfile() → repository → DAO → Drift → SQLCipher"
      pattern: "manual"
---

<objective>
Human verification of all Phase 1 success criteria on a physical Android device.
Run the full automated test suite, then walk through each success criterion manually.

Purpose: Biometric auth, encryption-at-rest, and photo picker cannot be fully
verified in automated tests. This checkpoint confirms all five Phase 1 success
criteria are met before marking the phase complete.

Output: Go/no-go decision for Phase 1 completion. Issues become gap closure tasks.
</objective>

<execution_context>
@/Users/teddericchan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/teddericchan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-secure-foundation/01-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full automated test suite</name>
  <files></files>
  <action>
Run the complete automated test suite and confirm everything is green:

```bash
cd /Users/teddericchan/autofill && flutter test test/unit/ test/widget/ --no-pub
```

If any tests fail, fix them before proceeding to the manual verification checkpoint.
This is a blocking gate — do not advance to the human checkpoint with red tests.

Also run the analyzer one final time:
```bash
cd /Users/teddericchan/autofill && flutter analyze lib/
```

Fix any errors. Warnings are acceptable.
  </action>
  <verify>
    <automated>cd /Users/teddericchan/autofill && flutter test test/unit/ test/widget/ --no-pub 2>&1 | tail -10 && flutter analyze lib/ 2>&1 | grep error | wc -l</automated>
  </verify>
  <done>
    `flutter test test/unit/ test/widget/` exits 0 with 0 failures.
    `flutter analyze lib/` shows 0 errors.
    All PROF-01 through PROF-09 and SEC-04 unit/widget tests are green.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Phase 1: Secure Foundation — encrypted profile storage, biometric auth
    gate, profile CRUD UI with photo/avatar, custom fields, and freemium 2-profile cap.
  </what-built>
  <how-to-verify>
Install the debug APK on a physical Android device:
```bash
flutter run --debug
```

Work through each success criterion:

**SC1 — Profile CRUD with all built-in fields:**
1. Authenticate to unlock the app
2. Tap the FAB to create a new profile
3. Fill in: display name, date of birth, address (all lines), phone, allergies, emergency contact name + phone
4. Select relationship tag (try "Child")
5. Tap "Pick photo" — select an image from the gallery
6. Save the profile
7. Verify: profile appears in list with the avatar photo, name, and relationship chip
8. Tap the profile to edit — verify all saved fields are visible and editable
9. Edit the display name and save — verify the change persists after going back to list
10. Delete the profile (confirm dialog) — verify it disappears from the list

**SC2 — Custom fields:**
1. Create a new profile (or edit an existing one)
2. Scroll to the custom fields section
3. Tap "Add field" — enter label "Student ID", type "text", value "12345"
4. Save — verify the custom field appears in the list
5. Edit the custom field — change the value to "67890" and save
6. Verify the updated value appears
7. Delete the custom field — verify it disappears
8. Verify the profile's built-in fields are unchanged

**SC3 — Biometric auth gate:**
1. Close the app completely (remove from recents)
2. Reopen the app
3. Verify: the profile list is NOT visible — only the Unlock screen
4. Tap Unlock (or wait for auto-prompt)
5. Verify: biometric prompt appears with reason "Unlock your family profiles"
6. Authenticate — verify profile list appears

**SC4 — Encryption at rest (manual DB check):**
1. With app installed and at least one profile saved, use adb:
```bash
adb shell run-as com.example.autofill ls files/
adb shell run-as com.example.autofill cat files/autofill.db | head -c 200 | xxd
```
2. Verify: output is binary/gibberish — NOT human-readable SQL or JSON
3. (Optional) Pull DB file and attempt to open with sqlite3 CLI without a key:
```bash
adb shell run-as com.example.autofill cat files/autofill.db > /tmp/autofill.db
sqlite3 /tmp/autofill.db ".tables"
```
Expected: "file is not a database" or "unable to open database" error

**SC5 — Freemium cap:**
1. Create two profiles (if not already done)
2. Tap FAB to create a third profile
3. Verify: upgrade prompt screen appears (NOT a crash, NOT a regular error)
4. Verify: upgrade prompt shows "Upgrade to Family Pro" message
5. Verify: "Upgrade" button exists (even though it's a stub in Phase 1)
6. Tap back — verify the two existing profiles are still intact
  </how-to-verify>
  <resume-signal>
    Type "approved" if all five success criteria pass.
    Describe any failures in detail so gap closure plans can be created.
  </resume-signal>
</task>

</tasks>

<verification>
All five Phase 1 success criteria verified on physical device:
1. Profile CRUD with all built-in fields + photo ✓
2. Custom fields add/edit/delete ✓
3. Biometric auth gate prevents access without unlock ✓
4. DB file unreadable without key ✓
5. Third profile creation shows upgrade prompt ✓
</verification>

<success_criteria>
- `flutter test test/unit/ test/widget/` exits 0 with 0 failures
- `flutter analyze lib/` shows 0 errors
- All 5 Phase 1 success criteria verified on physical Android device
- Human approves checkpoint with "approved"
</success_criteria>

<output>
After completion, create `.planning/phases/01-secure-foundation/01-07-SUMMARY.md`

If approved: update ROADMAP.md Phase 1 status to "Complete" and record completion date.
If gaps found: list them in the summary so /gsd:plan-phase --gaps can create closure plans.
</output>
