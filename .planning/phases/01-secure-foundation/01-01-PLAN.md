---
phase: 01-secure-foundation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - test/helpers/in_memory_database.dart
  - test/unit/database/encryption_test.dart
  - test/unit/database/schema_test.dart
  - test/unit/profile/profile_repository_test.dart
  - test/unit/profile/profile_usecase_test.dart
  - test/unit/profile/custom_field_repository_test.dart
  - test/widget/profile/profile_form_test.dart
  - integration_test/auth_gate_test.dart
autonomous: true
requirements: [SEC-04, PROF-01, PROF-02, PROF-03, PROF-04, PROF-05, PROF-06, PROF-07, PROF-08, PROF-09]

must_haves:
  truths:
    - "All test files exist and compile (even with failing/skipped stubs)"
    - "`flutter test test/unit/ test/widget/` runs without 'file not found' errors"
    - "Every test file has at least one pending/skip stub covering its requirement"
  artifacts:
    - path: "test/helpers/in_memory_database.dart"
      provides: "Shared in-memory Drift DB fixture for all unit tests"
    - path: "test/unit/database/encryption_test.dart"
      provides: "Test stubs for SEC-04 encryption path"
    - path: "test/unit/database/schema_test.dart"
      provides: "Test stubs for UUID PKs and sync fields"
    - path: "test/unit/profile/profile_repository_test.dart"
      provides: "Test stubs for PROF-01/02/03/05 CRUD"
    - path: "test/unit/profile/profile_usecase_test.dart"
      provides: "Test stubs for PROF-06 freemium cap"
    - path: "test/unit/profile/custom_field_repository_test.dart"
      provides: "Test stubs for PROF-07/08/09 custom fields"
    - path: "test/widget/profile/profile_form_test.dart"
      provides: "Widget test stub for PROF-04 avatar"
    - path: "integration_test/auth_gate_test.dart"
      provides: "Integration test stub for biometric gate"
  key_links:
    - from: "All test files"
      to: "test/helpers/in_memory_database.dart"
      via: "import"
      pattern: "import.*in_memory_database"
---

<objective>
Create all test scaffold files (Wave 0) so that every subsequent implementation task
has a waiting test stub to make green. Tests start as `skip` or `markTestSkipped` stubs
— they compile and run but do not fail.

Purpose: Nyquist rule compliance. Every implementation task in Waves 1-5 must have an
automated verify command. This plan pre-creates all test files so those commands exist
before implementation begins.

Output: 8 test files (stub content only), all compiling, `flutter test test/unit/ test/widget/` exits 0.
</objective>

<execution_context>
@/Users/teddericchan/.claude/get-shit-done/workflows/execute-plan.md
@/Users/teddericchan/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-secure-foundation/01-VALIDATION.md
@.planning/phases/01-secure-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test helper and database test stubs</name>
  <files>
    test/helpers/in_memory_database.dart,
    test/unit/database/encryption_test.dart,
    test/unit/database/schema_test.dart
  </files>
  <action>
Create three files. No real implementation — stubs only. All tests use `markTestSkipped` or `skip:` so they compile without the production code existing yet.

**test/helpers/in_memory_database.dart:**
Exports a helper function `Future<AppDatabase> openTestDatabase()` that opens an
in-memory Drift database (no encryption, no file I/O) using `NativeDatabase.memory()`.
Import the `AppDatabase` class path as a TODO comment since the class does not exist yet:
`// TODO: import 'package:autofill/data/database/app_database.dart';`
For now, export just the function signature as a comment block and a placeholder that
returns a `Future.error('not implemented')`. The key purpose is to establish the file
path and import structure so all test files can reference it.

**test/unit/database/encryption_test.dart:**
Create a `main()` with a `group('SEC-04 encryption', ...)` block containing these
test stubs (all with `skip: 'Wave 0 stub — implement after Wave 1'`):
- `'KeyManager generates a 32-byte base64 key on first call'`
- `'KeyManager returns the same key on subsequent calls'`
- `'AppDatabase opens successfully with valid key'`
- `'AppDatabase fails to open (or returns empty) when opened without correct key'`
Each test body calls `markTestSkipped('stub')`.

**test/unit/database/schema_test.dart:**
Create a `main()` with a `group('Schema shape', ...)` block:
- `'profiles table has UUID TEXT primary key (no AUTOINCREMENT)'`
- `'profiles table has createdAt, updatedAt, deletedAt, synchronized columns'`
- `'custom_fields table has UUID TEXT primary key'`
- `'custom_fields table has profileId FK, label, fieldType, value columns'`
- `'custom_fields table has createdAt, updatedAt, deletedAt, synchronized columns'`
All skip stubs. Import path as a TODO comment.
  </action>
  <verify>
    <automated>cd /Users/teddericchan/autofill && flutter test test/unit/database/ --no-pub 2>&1 | tail -5</automated>
  </verify>
  <done>
    Three files exist. `flutter test test/unit/database/` runs and exits 0 (all tests skipped, none failing).
  </done>
</task>

<task type="auto">
  <name>Task 2: Create profile and widget test stubs</name>
  <files>
    test/unit/profile/profile_repository_test.dart,
    test/unit/profile/profile_usecase_test.dart,
    test/unit/profile/custom_field_repository_test.dart,
    test/widget/profile/profile_form_test.dart,
    integration_test/auth_gate_test.dart
  </files>
  <action>
Create five stub test files. All tests use `skip:` so they compile without production code.

**test/unit/profile/profile_repository_test.dart:**
Group `'ProfileRepository'` with stubs:
- PROF-01: `'createProfile writes all built-in fields to DB'`
- PROF-02: `'updateProfile persists changes and increments updatedAt'`
- PROF-03: `'deleteProfile sets deletedAt (soft delete), not hard delete'`
- PROF-03: `'deleted profile excluded from countActive()'`
- PROF-05: `'relationshipTag persisted and retrieved for parent/child/guardian'`

**test/unit/profile/profile_usecase_test.dart:**
Group `'ProfileUseCase'` with stubs:
- PROF-06: `'createProfile succeeds when count < 2 on free tier'`
- PROF-06: `'createProfile returns ProfileLimitReached when count == 2 on free tier'`
- PROF-06: `'createProfile succeeds when count == 2 on paid tier'`

**test/unit/profile/custom_field_repository_test.dart:**
Group `'CustomFieldRepository'` with stubs:
- PROF-07: `'addCustomField creates row in custom_fields table linked to profile'`
- PROF-08: `'editCustomField updates label, fieldType, value, and updatedAt'`
- PROF-08: `'deleteCustomField sets deletedAt (soft delete)'`
- PROF-09: `'getProfileWithFields returns built-in fields and custom fields together'`

**test/widget/profile/profile_form_test.dart:**
Group `'ProfileForm widget'` with stubs:
- PROF-04: `'avatar picker button is present in form'`
- PROF-04: `'selecting image updates avatar preview'`
- PROF-06: `'third profile creation triggers paywall navigation'`

**integration_test/auth_gate_test.dart:**
Group `'AuthGate integration'` with stubs (skip note: 'physical device only'):
- `'app starts in locked state — profile list not visible'`
- `'successful biometric auth shows profile list'`
- `'failed biometric shows PIN fallback option'`
  </action>
  <verify>
    <automated>cd /Users/teddericchan/autofill && flutter test test/unit/profile/ test/widget/ --no-pub 2>&1 | tail -5</automated>
  </verify>
  <done>
    Five files exist. `flutter test test/unit/profile/ test/widget/` exits 0 (all skipped).
    `flutter test test/unit/ test/widget/` exits 0 (full stub suite).
  </done>
</task>

</tasks>

<verification>
Run the full unit + widget stub suite:
```
cd /Users/teddericchan/autofill && flutter test test/unit/ test/widget/ --no-pub
```
Expected: all tests skipped (0 failed, N skipped). Exit code 0.
</verification>

<success_criteria>
- 8 test files exist at correct paths
- `flutter test test/unit/ test/widget/` exits 0 with 0 failures
- Every requirement ID (SEC-04, PROF-01 through PROF-09) has at least one skip stub
- No test file imports a non-existent production class directly (use TODO comments for future imports)
</success_criteria>

<output>
After completion, create `.planning/phases/01-secure-foundation/01-01-SUMMARY.md`
</output>
